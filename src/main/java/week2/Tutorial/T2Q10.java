package week2.Tutorial;
/*
10.	When the compiler encounters a generic class, interface, or method with a bound type parameter,
    such as <T extends Number> or <E extends Comparable> ,
    it replaces all occurrences of the type parameter with what type?
    - For T it will replace with Number type parameter, and for E replace with Comparable type parameter
 */
public class T2Q10 {
}
